apiVersion: apps/v1
kind: Deployment
metadata:
  name: flag-service
  namespace: togglemaster
  labels: { app: flag-service }
spec:
  replicas: 2
  selector: { matchLabels: { app: flag-service } }
  template:
    metadata:
      labels: { app: flag-service }
    spec:
      containers:
        - name: flag-service
          image: 661099433763.dkr.ecr.us-east-1.amazonaws.com/togglemaster/flag-service:v1.0.0-465da44
          ports: [{ containerPort: 8002 }]
          env:
            - name: PORT
              value: "8002"
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: flag-secrets, key: database_url } }
            - name: AUTH_SERVICE_URL
              value: "http://auth-service.togglemaster.svc.cluster.local"
            # --- OpenTelemetry (instrumentacao automatica via opentelemetry-instrument) ---
            - name: OTEL_SERVICE_NAME
              value: "flag-service"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.observability.svc.cluster.local:4317"
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: "grpc"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.namespace=togglemaster,deployment.environment=hml"
            - name: OTEL_LOGS_EXPORTER
              value: "none"
          readinessProbe:
            httpGet: { path: /health, port: 8002 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8002 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 50m,  memory: 64Mi }
            limits:   { cpu: 250m, memory: 256Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: togglemaster
spec:
  selector: { app: flag-service }
  ports: [{ port: 80, targetPort: 8002 }]
